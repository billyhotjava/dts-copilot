# Sprint-33 集成验证（IT）

**状态**: IN_PROGRESS

汇总 Sprint-33 财务可证明正确性的可重跑验证证据。各 Feature 完成时回填，**禁止空占位**（沿用 sprint-30 `it/` 标准）。

本地 contract gate 最近重跑记录：`it/evidence/20260605-local/sprint33-local-it-gates-rerun.md`（全部 PASS；live 接线缺口仍按各项 IN_PROGRESS 保留）。
F2 应用 MySQL 权威基准证明链路记录：`it/evidence/20260606-local/f2-application-mysql-authority-proof.md`（PASS；dbt 包保存在 `dts-copilot/worklog/prs/v1`，不放入 `dts-stack`；runtime HTTP smoke 推荐使用 `it/test_f2_application_mysql_authority_runtime_http.sh`，本地端到端直连证明推荐使用 `it/test_f2_application_mysql_authority_runtime_live_local.sh`，已登记应用 MySQL 证明推荐使用 `it/test_f2_application_mysql_authority_runtime_registered_datasource.sh`）。
2026-06-07 本地复核：除需要 live HTTP/运行中容器/平台数据源密码的 runtime 脚本外，15 个本地 contract gate 全部 PASS；补充 `it/test_sprint33_worklog_consistency.sh` 锁定 F1-T01/IT-01 与 Sprint 顶层完成标准同步，同时保持 live L2 明细对账未完成项未勾选；新增 `it/test_f1_authority_route_preflight.sh` 将 F1 legacy L2 权威基准 route 条件固化为可重跑预检，默认输出 `PENDING_LIVE_EVIDENCE`，强门禁用 `REQUIRE_LIVE_F1_AUTHORITY_ROUTE=true`；F2 主 proof contract 已强制引用入湖映射 preflight，且当前平台 `ptr_mysql_flow` 源表/ODS 目标表/`table_mapping` 覆盖 F2 六张财务表；live L2/adminapi 接线、真实凭证科目 tie-out、持续对账发布和财务签字缺口不因此变更，Sprint 仍为 IN_PROGRESS。
2026-06-07 完成度总门禁：`it/test_sprint33_completion_readiness.sh` 汇总 Sprint-33 顶层完成标准、IT matrix、F1 route preflight、F2 live tie-out、F4 live scorecard 和 F5 签字状态；默认输出 `status=IN_PROGRESS` 和 pending 清单，设置 `REQUIRE_SPRINT33_DONE=true` 后作为强 DONE 门禁失败。
2026-06-07 本地 contract 聚合：`it/test_sprint33_local_contracts.sh` 一键串起当前非 live 强依赖的 Sprint-33 contract gates，包括 worklog consistency、completion readiness、authority registry naming、application MySQL authority naming、F1 authority route 诊断契约、F1/F2/F3/F4/F5 本地证明、F2 入湖映射 preflight、runtime 多 case contract、F4 scorecard live preflight 和签字基线 contract；live route / live scorecard / 真实签字仍由 readiness gate 保持 pending。
2026-06-07 F1 route 诊断复核：`it/test_f1_authority_route_preflight_contract.sh` 锁定缺 live base URL 时必须输出运行态诊断；当前机器实际 `it/test_f1_authority_route_preflight.sh` 输出 `diagnostic.legacy_adminapi_containers=missing`、`diagnostic.dts_admin_rs_flowers_route=missing`、`diagnostic.host_port_8000=in_use`，证明仍需提供 legacy `adminapi/rs-gateway` 或 `/flowers-dev-api` 等价入口。
2026-06-07 F4 scorecard 运行态预检：`it/test_f4_scorecard_live_preflight_contract.sh` 锁定 actuator/publish 诊断 contract；当前机器实际 `it/test_f4_scorecard_live_preflight.sh` 显示 `health_http_status=200`、`diagnostic.finance_reconciliation_health=present`、`diagnostic.finance_reconciliation_status=UP`。默认不执行会写快照的 POST；显式设置 `RUN_LIVE_F4_SCORECARD_PUBLISH=true` 并提供 `FINANCE_RECONCILIATION_SCORECARD_AUTHORIZATION`、`COPILOT_API_KEY` 或 `COPILOT_ADMIN_SECRET` 后才执行 live publish；使用 `COPILOT_ADMIN_SECRET` 时脚本会临时创建并撤销 API key，不打印原始 key。2026-06-07 重建 `dts-copilot-ai` 后，`publish-scheduled` 已从 404 修复为 `publish_http_status=200`；`docker-compose.yml` 已默认启用 honest local readiness provider，当前返回 `diagnostic.publish_status=PENDING_LIVE_EVIDENCE`、`diagnostic.published_count=0`、`diagnostic.skipped_reason_code=PENDING_LIVE_EVIDENCE`、`reason=scorecard_publish_pending_live_evidence`，说明 provider 已注册但 F1 live 权威基准证据仍缺，publisher 未写入 scorecard snapshot，F4 live scorecard 仍不能标记完成；证据见 `it/evidence/20260607-local/f4-scorecard-live-preflight.md`。
2026-06-07 authority registry 命名迁移：`it/test_sprint33_authority_registry_naming.sh` 锁定 canonical Java service 为 `FinanceAuthorityRegistry`，canonical 治理资产为 `finance-authority-registry.v1.json`；旧 `FinanceOracleRegistry` 只允许作为 deprecated 兼容外壳且不得作为 Spring service bean。
2026-06-07 application MySQL authority 命名迁移：`it/test_sprint33_application_mysql_authority_naming.sh` 锁定 canonical Java service 为 `FinanceApplicationMysqlAuthorityRegistry`，canonical JDBC configuration 为 `FinanceApplicationMysqlAuthorityJdbcConfiguration`，canonical REST 主入口为 `FinanceApplicationMysqlAuthorityProofResource`，canonical 治理资产为 `finance-application-mysql-authority-sql.v1.json`；`test_f2_application_mysql_authority_runtime_*.sh` 为当前 runtime proof 真实实现入口，legacy runtime 文件名仅作兼容 wrapper；旧 `FinanceApplicationMysqlOracleRegistry`、旧 JDBC configuration、旧 REST resource 和旧资源名只允许作为 deprecated 兼容外壳/兼容资产。

## 证据清单（待回填）

| 编号 | 验证项 | 来源 | 重跑方式 | 状态 |
|------|--------|------|----------|------|
| IT-00 | Sprint-33 完成度总门禁 | Sprint README + IT matrix + F1/F2/F4/F5 live/signoff gates | `it/test_sprint33_completion_readiness.sh`（强门禁：`REQUIRE_SPRINT33_DONE=true it/test_sprint33_completion_readiness.sh`） | IN_PROGRESS（默认汇总 pending 清单；当前仍有 live L2/adminapi、真实凭证科目 tie-out、F4 live scorecard 和财务/审计签字缺口） |
| IT-LOCAL | Sprint-33 本地 contract 聚合 | IT-01..IT-11 非 live 强依赖合同 | `it/test_sprint33_local_contracts.sh` | PASS（本地一键入口；不把 `PENDING_LIVE_EVIDENCE` 或 `PENDING_SIGNATURE` 伪造成 DONE） |
| IT-01 | 财务权威基准注册表 | F1-T01 | `it/test_sprint33_finance_authority_registry.sh` + `it/test_sprint33_authority_registry_naming.sh` | PASS（2026-06-05 local；2026-06-07 追加 registry 运行日志和下游缺失/不对齐错误均使用 `authority binding` 文案；canonical service/治理资产已迁到 `FinanceAuthorityRegistry` / `finance-authority-registry.v1.json`，旧 Oracle 命名仅作 deprecated 兼容外壳） |
| IT-02 | 明细级对账（copilot vs L2 端点逐额） | F1-T02/T03 | `it/test_f1_detail_reconciliation.sh` + `it/test_f1_amount_column_alignment.sh` + `it/test_f1_authority_route_preflight_contract.sh` + `it/test_f1_authority_route_preflight.sh` + `it/evidence/20260605-local/f1-live-authority-auth-precheck.md` + `it/evidence/20260605-local/f1-live-authority-admin-session-route-precheck.md` | IN_PROGRESS（core comparator + source orchestration + JSON payload adapter + HTTP provider contract + legacy route 误配提示 + live route diagnostic contract + live route preflight + live sample SQL contract + amount tier alignment PASS；HTTP provider 推荐 `authority-base-url/authority-authorization/authority-cookie` 指向 L2 权威基准入口，并兼容旧 `oracle-*` 配置；`test_f1_authority_route_preflight.sh` 默认在未配置/不可达时输出 `PENDING_LIVE_EVIDENCE` 和 legacy 容器/Traefik route/8000 端口诊断，设置 `REQUIRE_LIVE_F1_AUTHORITY_ROUTE=true` 才作为强门禁失败；明细失败信息使用 `authorityBindingId`/`authority` 文案，缺失/重复权威基准行均不再输出 Oracle 数据库语义；admin session auth 已验证可访问 `/api/**`，但当前 `dts-admin` 不承载 legacy `/rs-flowers-base/...` L2 权威基准 route，需真实 `adminapi/rs-gateway` base URL） |
| IT-03 | 汇总双路对账 | F2-T02 | `it/test_f2_summary_dual_reconciliation.sh` + `it/test_sprint33_application_mysql_authority_naming.sh` + `it/test_f2_application_mysql_authority_proof.sh` + `it/test_f2_finance_ingestion_mapping_preflight.sh` + `it/test_f2_application_mysql_authority_runtime_http.sh` + `it/test_f2_application_mysql_authority_runtime_live_local.sh` + `it/test_f2_application_mysql_authority_runtime_registered_datasource.sh` + `worklog/prs/v1/tests/test_xycyl_finance_dbt_zip_contract.sh` + `it/evidence/20260606-local/f2-application-mysql-authority-proof.md` | IN_PROGRESS（summary comparator + case registry + SQL 方言 contract PASS，汇总失败信息使用 `authority` 文案；应用 MySQL proof canonical registry/治理资产/JDBC configuration/REST resource 已迁到 `FinanceApplicationMysqlAuthorityRegistry` / `finance-application-mysql-authority-sql.v1.json` / `FinanceApplicationMysqlAuthorityJdbcConfiguration` / `FinanceApplicationMysqlAuthorityProofResource`，旧 Oracle 命名仅作 deprecated 兼容外壳/兼容资产/兼容 alias；月对账/售账/2026 凭证 ADS vs 应用 MySQL 原表权威基准 SQL contract + 应用内 proof runner/resource + F2 scorecard lane `application-mysql-authority-proof` 桥接 + JDBC executor/config + runtime HTTP smoke PASS；应用 MySQL proof 已支持推荐 `application-mysql-authority` 配置/REST 路径/`authorityBindingId` 响应字段和旧 `application-mysql-oracle`/`oracleBindingId` 兼容回退；运行结果、scorecard failure、JDBC 异常和 registry 加载日志均使用 authority 文案；v1 dbt zip 已补售账链和月对账 `projectId`；平台入湖任务映射 preflight 会检查 `ptr_mysql_flow` 源表、ODS 目标表和 `table_mapping` 覆盖 F2 proof 所需表，避免缺 ODS 入湖映射时静默运行；planner 已防 `prs.finance.voucher.profile` 抢走 TPL-57 ADS 模板；凭证权威基准 SQL 已按真实 `YYYYMM` 账期与凭证头口径修正；直连应用 MySQL 本地 proof 支持 `COPILOT_FINANCE_PROOF_CASE_IDS=all`，并有多 case contract 防止退化成只跑默认 case；已登记 `ptr_mysql` 通过平台入湖任务源回退，当前 `COPILOT_FINANCE_PROOF_CASE_IDS=all` 对月对账、售账、2026 凭证三 case 均返回 `PASSED`；live L2/签字 SQL vs copilot dataset 双路取数待接线） |
| IT-04 | 复式凭证 tie-out（借=贷） | F2-T01/T03 | `assets/voucher-tieout-mapping.md` + `it/test_sprint33_voucher_tieout_mapping.sh` + `it/test_f2_voucher_subject_tieout.sh` | IN_PROGRESS（T01 mapping/self-check PASS；T03 subject tie-out contract PASS；live 科目树/真实凭证 tie-out 待签字/接线） |
| IT-05 | 8 条不变量回归（结果级，任意条件） | F3-T01/T02 | `it/test_sprint33_finance_invariant_registry.sh`（T01）；`it/test_f3_invariant_regression.sh`（T02） | PASS（2026-06-05 local；离线代表性网格，live 权威基准/differential 归 F1/F4） |
| IT-06 | 静态口径 guardrail 拦截（链不混/坏账/去重） | F3-T03 | `it/test_sprint33_static_caliber_guardrails.sh` | PASS（2026-06-05 local；生成层 + analytics 执行前 gate） |
| IT-07 | 差分网格（copilot vs 权威基准） | F4-T01 | `it/test_f4_differential_grid.sh` | IN_PROGRESS（representative grid registry + comparator contract + summary SQL row provider PASS，差分失败信息使用 `authority` 文案；本地行源可按项目/账期/日期区间切片映射已登记 summary SQL，未知过滤条件直接失败；live copilot `/api/dataset` vs adminapi 权威基准端点待接线） |
| IT-08 | 持续对账记分卡 + 漂移告警 | F4-T02 | `it/test_sprint33_reconciliation_scorecard.sh` + `it/test_f4_scorecard_live_preflight_contract.sh` + `it/test_f4_scorecard_live_preflight.sh` + `it/evidence/20260607-local/f4-scorecard-live-preflight.md` | IN_PROGRESS（scorecard policy + four-lane pass-rate/drift contract + 最新快照 store/source + publisher 完整证据门禁 + publish result 推荐 `authorityBindingId` 并兼容旧 `oracleBindingId` + schedule provider 骨架 + REST 按需触发入口 + scheduled publisher skip reason code + local readiness provider 默认关闭且缺 live lane 显式 `PENDING_LIVE_EVIDENCE` + compose 默认启用 honest local readiness provider + 应用 MySQL proof 可转 F2 scorecard check + 应用 MySQL scorecard evidence provider 可选输出真实 F2 lane + 差分网格 scorecard evidence provider 可选输出真实 F4 lane + summary SQL row provider 可作为 F4 本地行源 + 签字 baseline 才可压制重复漂移 + actuator `financeReconciliation` health field PASS + live publish endpoint 200；F4 live publish 当前返回 `PENDING_LIVE_EVIDENCE/published_count=0/PENDING_LIVE_EVIDENCE`，原因是 provider 已注册但 F1 live 权威基准证据仍缺，publisher 未写入 snapshot，F4 live 运行参数/数据源配置和真实签字待接线） |
| IT-09 | 财务回答可审计溯源样例 | F5-T01 | `it/test_f5_finance_answer_audit_trail.sh` | IN_PROGRESS（backend audit package + chat/SSE `trace.financeAudit.authorityStatus` 自动接线，旧 `oracleStatus` 仅作兼容 + scorecard snapshot source/publisher + web TracePanel/stream/legacy response contract PASS；scorecard source 异常日志使用 `authorityBindingId` 文案；真实 live scorecard 发布运行、财务回答抽样与审计复跑待补） |
| IT-10 | 财务签字基线 | F5-T02 | `it/test_f5_finance_signoff_baseline.sh` + `assets/finance-signoff-baseline.md` | IN_PROGRESS（engineering evidence package + REST build/list contract PASS；未签字 baseline 不作为 scorecard 已接受差异；真实财务/审计签字待补，不伪造签字） |
| IT-11 | 弱路径财务 telemetry 入对账候选 | F4-T03 | `it/test_f4_weak_path_reconciliation_candidates.sh` | IN_PROGRESS（weak-path finance candidate + scorecard drift contract + candidate snapshot store + local semantic draft staging + scheduled publisher PASS；真实 live telemetry 样本复核和 Sprint-31 F3 治理审核提交待接线） |

## 目录约定

```
it/
  README.md
  evidence/{YYYYMMDD-env}/
  sql/
  test_*.sh
```
