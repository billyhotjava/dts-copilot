# T04: "一键坏账处理单草稿"端到端验证

**优先级**: P1
**状态**: DONE
**依赖**: T03, F2/T03

## 目标

跑通完整闭环并留证：问答识别坏账风险 → 建议动作 → 用户确认 → 调 saveDraftFlowerBadDebt → 草稿单在 adminweb 可见待确认 → 审计链路完整。

## 技术设计

- 端到端脚本/手测：以一个真实命中"坏账风险"signal 的报花为输入。
- 走完：预警分支返回 advice + linkedActions → 卡片 → 确认 → 草稿创建 → adminweb 报花-坏账页出现待确认草稿。
- 验证正式提交仍需 adminweb 人工点 saveFlowerBadDebt（copilot 不代提交）。

## 影响范围

- `it/`、`it/evidence/` 新增端到端验证记录与审计链路截图/日志。

## 验证

- [x] 端到端闭环跑通，草稿在 adminweb 可见。
- [x] copilot 全程未触碰 commit 端点与业务库。
- [x] 审计链路（who/when/action/草稿号）完整留证。

## 完成标准

- [x] Tier3 闭环端到端证据入 IT，安全姿态被验证。

## 当前进展

- 2026-05-30 重试已启动本地 old PRS `rs-gateway` / `rs-auth` / `rs-system` / `rs-flowers-base` 最小栈。
- `/code`、`/auth/login`、带 `Authorization` 的 `/rs-flowers-base/flower/bizBadDebt/listPage` 均已跑通。
- 真实 `POST /rs-flowers-base/flower/bizBadDebt/saveDraftFlowerBadDebt` 已返回 `code=200`，生成 `bizType=6`、`status=20` 草稿，DB 草稿数 `0 -> 1`。
- 已把 `COPILOT_ACTION_ADMINAPI_BASE_URL` / `COPILOT_ACTION_ADMINAPI_AUTHORIZATION` 注入并重建 `dts-copilot-ai` 运行容器，`RUN_LIVE=1` 的 `test_action_runtime_env_wiring.sh` 通过。
- 最新运行态验证生成草稿 `2060736510340108288`，marker=`copilot-runtime-t04-20260530225405`；Copilot 审计日志 `id=326`，`ACTION_EXECUTION`，`success=true`，guard=`flowerbiz:baddebt:draft`。
- adminweb 坏账列表页调用的 `/rs-flowers-base/flower/bizBadDebt/listPage` 已返回该草稿，`total=4`，最新行 `status=20`、`bizType=6`、`badDebtType=1`。
- 正式提交端点 `saveFlowerBadDebt` 未调用，仍保持 copilot 只落草稿、不代提交的安全边界。
- 证据：`it/evidence/20260530-local/baddebt-e2e-auth-blocker.md`。
- 运行态证据：`it/evidence/20260530-local/action-runtime-live.md`。

## 说明

- 本次以 adminweb 坏账列表页同源 `listPage` 接口返回作为可见性证据；未单独构建 old PRS adminweb 截图。
