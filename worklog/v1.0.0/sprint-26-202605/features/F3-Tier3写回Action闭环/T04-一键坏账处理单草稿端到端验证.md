# T04: "一键坏账处理单草稿"端到端验证

**优先级**: P1
**状态**: BLOCKED
**依赖**: T03, F2/T03

## 目标

跑通完整闭环并留证：问答识别坏账风险 → 建议动作 → 用户确认 → 调 saveDraftFlowerBadDebt → 草稿单在 adminweb 可见待确认 → 审计链路完整。

## 技术设计

- 端到端脚本/手测：以一个真实命中"坏账风险"signal 的报花为输入。
- 走完：预警分支返回 advice + linkedActions → 卡片 → 确认 → 草稿创建 → adminweb 报花-坏账页出现待确认草稿。
- 验证正式提交仍需 adminweb 人工点 saveFlowerBadDebt（copilot 不代提交）。

## 影响范围

- `it/`、`it/evidence/` 新增端到端验证记录与审计链路截图/日志。

## 验证

- [ ] 端到端闭环跑通，草稿在 adminweb 可见。
- [ ] copilot 全程未触碰 commit 端点与业务库。
- [ ] 审计链路（who/when/action/草稿号）完整留证。

## 完成标准

- [ ] Tier3 闭环端到端证据入 IT，安全姿态被验证。

## 当前阻塞

- 2026-05-30 重试确认：当前手工探测的 `http://127.0.0.1:38012` 是 v2.2.3 `dts-admin`，不是 PRS 旧 `rs-gateway`；`/rs-flowers-base/**` 带有效三员 token 后返回 `403`，说明路径被 dts-admin 安全配置拒绝。
- `dts-copilot-proxy` 只路由 copilot 自身 `/api/ai`、`/api`、`/`，裸 `/rs-flowers-base/**` 会落到 webapp 并返回 `405`。
- 当前本机未运行旧 PRS `rs-gateway` / `rs-flowers-base` 容器；端到端需要先配置正确的 `copilot.action.adminapi.base-url` 和该 adminapi 接受的业务 Authorization。
- 已补 `copilot.action.adminapi.authorization` 配置入口，并补 `copilot.action.adminapi.base-url` 缺失时的 fail-fast 保护；聊天 `/approve`/`/cancel` 已接到审批服务。拿到正确 base URL + 业务 token 后可重跑端到端草稿创建。
- 阻塞证据：`it/evidence/20260530-local/baddebt-e2e-auth-blocker.md`。
