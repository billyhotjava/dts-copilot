# T01: 路由阶梯到数据层映射

**优先级**: P0
**状态**: DONE
**依赖**: F1-T01

## 目标

把 Sprint-32 的路由阶梯映射为准确性证据中的 `routeTier` 和 `dataSurface`。

## 技术设计

- 指标/平台指标：`T1_METRIC`。
- ADS/DWS 模板：`T2_ADS_TEMPLATE`。
- 对象图导航：`T3_OBJECT_GRAPH`。
- 受控联邦 SQL：`T4_GUARDED_FEDERATION`。
- 直连/自由 SQL/L0 profile：`T5_WEAK_PATH`。

## 影响范围

- Template matcher、planner policy、route telemetry。
- accuracy evidence assembler。

## 验证

- [x] 财务凭证模板命中 `T2_ADS_TEMPLATE`。
- [x] L0 profile 命中 `T5_WEAK_PATH`。
- [x] 平台指标命中 `T1_METRIC`。

## 完成标准

- [x] 所有 NL2SQL 路由分支都能映射到明确阶梯。
