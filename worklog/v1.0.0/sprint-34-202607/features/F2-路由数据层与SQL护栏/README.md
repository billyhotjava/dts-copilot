# F2: 路由数据层与 SQL 护栏

**优先级**: P0
**状态**: DONE

## 目标

把“走了哪层数据”和“SQL 是否符合口径/安全边界”变成可检查证据，避免弱路径结果伪装成高可信统计。

## Task 列表

| ID | Task | 优先级 | 状态 | 依赖 |
|----|------|--------|------|------|
| T01 | 路由阶梯到数据层映射 | P0 | DONE | F1-T01 |
| T02 | ADS/DWS 白名单与 L0 硬降级 | P0 | DONE | T01 |
| T03 | SQL 静态口径 guardrail | P1 | DONE | T02 |

## 完成标准

- [x] 每次查询都有 `routeTier` 和 `dataSurface`。
- [x] L0 profile 和应用库直连不能被标高可信。
- [x] SQL 只读、白名单、参数绑定和口径 guardrail 被记录。
