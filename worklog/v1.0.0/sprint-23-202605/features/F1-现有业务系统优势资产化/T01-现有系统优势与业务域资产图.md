# T01: 现有系统优势与业务域资产图

**优先级**: P0  
**状态**: DONE  
**依赖**: sprint-22 报花 dbt / screen 资产

## 目标

形成 Sprint-23 的业务资产输入：把 PRS 现有应用系统优势整理成 Agent BI 可以路由、取数、生成报表的业务域。

## 输出物

`assets/application-advantages-and-domain-map.md`

必须包含：

1. `adminapi` 业务模块优势：报花、项目、客户、采购、库存、财务、任务、审批。
2. `adminweb` 页面资产优势：业务菜单、固定报表、报表配置、租赁月报、帆软/JimuReport。
3. `dts-copilot` 智能层优势：语义包、模板快路径、`REPORT_DRAFT`、`analysis_draft`、图表/大屏。
4. 业务域优先级：P0/P1/P2。
5. 每个业务域的首选取数面：L2 固定报表、L1 dbt ADS/DWS、L0 adminapi 只读。
6. 首批自然语言问题清单。

## 验证

- [x] 资产图引用真实路径，不出现无法落地的虚构模块。
- [x] 租赁/报花域明确作为 Sprint-23 首个验收域。
- [x] 每个业务域都给出可生成的报表类型，而不是只列业务名称。

## 完成标准

- [x] F2 可以直接引用资产图生成 Agent BI 报表目录。
