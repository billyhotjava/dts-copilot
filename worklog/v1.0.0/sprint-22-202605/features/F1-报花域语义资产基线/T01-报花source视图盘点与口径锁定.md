# T01: 报花 source / 视图盘点与口径锁定

**优先级**: P0
**状态**: READY
**依赖**: F0-T02, F0-T03

## 目标

为 F1-T02/T03/T04 与 F4 提供"报花域单一事实来源（SOT）"：7 张源表 + 8 张目标 ads mart 的字段口径完整对照表。

## 输出物

`assets/flowerbiz-source-catalog.md` —— 报花 7 张源表 + 8 张 ads mart 完整字段口径文档。

包含：

1. **7 张源表清单**（字段 / 类型 / 业务含义 / 状态枚举值 / 时间字段含义 / 软外键关系）
2. **8 张 ads mart 清单**（粒度 / 关键字段 / 物化策略 / 上游依赖 / 主要回答的问句类型）
3. **问句决策表**（"如果问句包含 X，去 mart Y"）
4. **6 个真实陷阱在每张 mart 中的应对**（异步触发、软外键、状态竞态、金额符号、链路分叉、时间不可靠）
5. **F0-T03 的 5 个口径决策的具体落地映射**

## 与 F4-T01 的协作

F4-T01 实施 dbt sources 时直接引用本文档，避免再次"考古"业务。

## 验证

- [ ] 7 张源表字段与生产 `INFORMATION_SCHEMA` 1:1 对齐
- [ ] 8 张 ads mart 设计与 F4-T02 完全对齐
- [ ] 问句决策表覆盖 F0-T01 高频问句 TOP 30

## 完成标准

- [ ] catalog 文档可作为 F1-T02/T03/T04 + F4-T01/T02 的共同输入
