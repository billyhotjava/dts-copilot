# 报花域口径决策表（待业务方拍板）

> sprint-22 F0-T03 输出物模板。F1-T02 / F4-T02 实施前必须填完。

## 决策 1：报花的"业务时间"是什么？

**问题**：当业务方说"本月报花"时，"本月"对应哪个时间字段？

| 选项 | 字段 | 业务含义 | 示例数据 |
|---|---|---|---|
| A | `apply_time` | 申请时间 | 2025-02-28 申请 → 算 2 月 |
| B | `start_lease_time` | 起租时间 | 2025-03-15 起租 → 算 3 月（推荐）|
| C | `settlement_time` | 结算时间 | 2025-04-05 结算 → 算 4 月 |
| D | `finish_time` | 完工时间 | |
| E | `create_time` | 创建时间 | |

**业务方填**：

- 拍板：[ ]
- 拍板人：
- 拍板时间：
- 例外（如有）：
- 理由：

## 决策 2：13 bizType 在 mart 里怎么分组？

**问题**：每种 bizType 单独 mart 太碎，全合一张容易聚合错。

| 选项 | 分组方式 | mart 数量 |
|---|---|---|
| A | 每种独立 | 13 张 |
| B | 5 类合并：lease(1/2/3/4) / sale(7) / gift(8) / baddebt(6) / material(10/11/12) | 5-8 张（推荐）|
| C | 不聚合，所有 bizType 混一张 | 1 张 |

**业务方填**：

- 拍板：[ ]
- 拍板人：
- 拍板时间：
- 理由：

## 决策 3：异步联动的"数据滞后"如何处理？

**问题**：报花单 status=2 后 PlanPurchaseItem 异步生成；用户查"待入库的报花"是否能看到刚审完的？

| 选项 | 策略 |
|---|---|
| A | mart 物化时强制 JOIN 上下游（数据准确但有滞后）|
| B | 分两个 mart：含未结算 + 仅已结算（推荐）|
| C | 实时穿透 adminapi（准确但慢且耦合）|

**业务方填**：

- 拍板：[ ]
- 拍板人：
- 拍板时间：

## 决策 4：金额符号在 mart 层规范化吗？

**问题**：bizType=2 `biz_total_rent` 是正数，bizType=3 是负数。

| 选项 | 处理 |
|---|---|
| A | 保留原符号（让 NL2SQL 自己判断）|
| B | 统一变正 + amount_direction 字段（推荐）|
| C | 拆为 amount_in / amount_out 两列（同 B 但更直观）|

**业务方填**：

- 拍板：[ ]
- 拍板人：
- 拍板时间：

## 决策 5：销售/赠花是否进财务 mart？

**问题**：bizType=7/8 走 ISaleAccountService，**不进** SettlementItem。是否在报花 mart 统一聚合？

| 选项 | 处理 |
|---|---|
| A | 完全独立的 sale_summary mart（推荐）|
| B | 合并到 lease_summary |
| C | 提供 UNION 视图（A + 可选 UNION）|

**业务方填**：

- 拍板：[ ]
- 拍板人：
- 拍板时间：

## 后续可能的次级决策

- 报花单"过期"判定阈值（"审核中超过 N 天"的 N 是多少？）
- 客户"新签"判定（首次报花 vs 首次合同）
- 养护人"工作量"包含哪些 bizType（仅租赁？还是含销售？）
- 跨年报花（2024 起租 2025 完工的）算哪年？

待业务方在实施过程中按需拍板。
