# T02: 项目实摆与摆位调整数据画像

**优先级**: P0  
**状态**: BLOCKED  
**依赖**: T01

## 目标

对 `p_project_green` 和 `p_position_adjustment*` 跑数据画像，验证状态枚举、父子行、数量和金额字段是否支持项目域 mart 建模。

## 阻塞

2026-05-29 本地 `biadmin.public` 未发现 `ods_ptr_mysql_p_project_green`、`ods_ptr_mysql_p_position_adjustment`、`ods_ptr_mysql_p_position_adjustment_item`。这些表入湖前，本 task 只能保留画像 SQL，不能给出业务口径结论。

## 画像项

- `p_project_green.status`、`import_status`、`parent_id` 分布。
- `rent`、`cost`、`total_number`、`good_number` 空值率和异常值。
- `project_id` / `position_id` / `good_price_id` 软外键孤儿率。
- `p_position_adjustment.status`、`adjustment_type` 实际分布。
- `p_position_adjustment_item.position_adjustment_id` 孤儿率。

## 验证

- [ ] 画像 SQL 输出归档到 `it/evidence/<date>/project-profile.sql.log`。
- [ ] 画像结论回写到 `assets/project-caliber-decisions.md`。

## 完成标准

- [ ] 能解释实摆组数应该按行数、顶层行还是 `total_number` 聚合。
- [ ] 能给出 dbt schema.yml 中哪些 relationships 应设为 warn。
