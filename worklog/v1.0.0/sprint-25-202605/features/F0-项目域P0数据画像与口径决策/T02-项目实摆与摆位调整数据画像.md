# T02: 项目实摆与摆位调整数据画像

**优先级**: P0  
**状态**: DONE
**依赖**: T01

## 目标

对 `p_project_green` 和 `p_position_adjustment*` 跑数据画像，验证状态枚举、父子行、数量和金额字段是否支持项目域 mart 建模。

## 画像结论

2026-05-30 task `46` 入湖后，`p_project_green=36295`、`p_position_adjustment=6016`、`p_position_adjustment_item=21009`。画像证据见 `it/evidence/20260530-local/project-profile-after-ingestion.md`。

关键事实：

- `p_project_green.status` 当前只出现 `1=摆放中` 和文档外 `-1`，其中 `-1` 为 2 行。
- `import_status=2` 为 36266 行，`import_status=1` 为 29 行。
- `parent_id=-1` 顶层行 16086 行，子项 20209 行，组数口径必须业务拍板。
- `rent` 有 4268 行空值，`cost` 无空值；raw 合计为 `rent=623546.44`、`cost=1879939.26`。
- `p_project_green` DWD 中项目软外键孤儿 286 行，物品价格软外键孤儿 1449 行，relationships 测试保持 warn。
- `p_position_adjustment_item` 无主表孤儿，旧摆位孤儿 264 行，新摆位孤儿 245 行。

## 画像项

- `p_project_green.status`、`import_status`、`parent_id` 分布。
- `rent`、`cost`、`total_number`、`good_number` 空值率和异常值。
- `project_id` / `position_id` / `good_price_id` 软外键孤儿率。
- `p_position_adjustment.status`、`adjustment_type` 实际分布。
- `p_position_adjustment_item.position_adjustment_id` 孤儿率。

## 验证

- [x] 画像 SQL 输出归档到 `it/evidence/20260530-local/project-profile-after-ingestion.md`。
- [x] 画像结论回写到 `assets/project-caliber-decisions.md`。

## 完成标准

- [x] 能解释实摆组数应该按行数、顶层行还是 `total_number` 聚合。
- [x] 能给出 dbt schema.yml 中哪些 relationships 应设为 warn。
