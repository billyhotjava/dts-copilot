# T03: 5 个项目域口径决策访谈

**优先级**: P0  
**状态**: BLOCKED  
**依赖**: T01, T02

## 目标

让业务方拍板 `assets/project-caliber-decisions.md` 中 5 个 P0 决策，作为 F1 dbt 建模的硬入口。

## 阻塞

依赖 T02 的实摆数据画像。缺少 `ods_ptr_mysql_p_project_green` 时，不能用实际分布支撑“组数、金额、快照粒度”等决策。

## 决策清单

- `p_project_green` 快照粒度。
- `rent` / `cost` 聚合是否乘数量。
- 停用项目是否计入默认汇总。
- 实摆“组数”定义。
- 项目域默认业务时间。

## 验证

- [ ] 每个决策有拍板人、时间和示例。
- [ ] 决策表状态从 `PENDING` 改为 `RESOLVED`。

## 完成标准

- [ ] F1 可以按决策表直接写 dbt，不需要开发自行猜口径。
