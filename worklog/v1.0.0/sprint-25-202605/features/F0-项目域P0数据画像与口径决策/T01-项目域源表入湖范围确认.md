# T01: 项目域源表入湖范围确认

**优先级**: P0  
**状态**: DONE  
**依赖**: 无

## 目标

确认 Sprint-25 所需项目域源表是否已经在默认数据湖形成 `public.ods_ptr_mysql_*` 表，避免 dbt sources 误指向不存在的 schema 或临时直连业务库。

## 范围

- 共享维度：`p_customer`、`p_project`、`p_contract`、`p_position`、`p_floor_layer`、`p_floor_number`、`b_goods`、`b_goods_price`
- 项目事实：`p_project_green`、`p_position_adjustment`、`p_position_adjustment_item`

## 验证

- [x] 在目标数据湖执行 `SELECT to_regclass('public.ods_ptr_mysql_p_project')` 等表存在性检查。
- [x] 确认每张 ODS 表包含 `_dts_source_system` / `_dts_import_time` 或记录缺口。
- [x] 将结果回填 `assets/project-source-catalog.md` 的 P0 状态列。

## 完成标准

- [x] 所有必需源表状态为“已确认”或“BLOCKED: 未入湖”。
- [ ] 未入湖表有明确补数责任人和预计完成时间。

## 2026-05-29 本地验证结论

- FOUND：`public.ods_ptr_mysql_p_project`（240 rows）、`public.ods_ptr_mysql_p_customer`（178 rows），两张表都有 `_dts_source_system` / `_dts_import_time`。
- MISSING：`public.ods_ptr_mysql_p_project_green`、`public.ods_ptr_mysql_p_position`、`public.ods_ptr_mysql_p_contract`、`public.ods_ptr_mysql_b_goods`、`public.ods_ptr_mysql_b_goods_price`、`public.ods_ptr_mysql_p_position_adjustment*` 等。
- 证据：`it/evidence/20260529-local/project-source-profile.md`。
