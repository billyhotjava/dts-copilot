# T03: 保存晋升后的来源状态回传

**优先级**: P1
**状态**: DONE
**依赖**: T02

## 目标

让草稿、正式查询和消费面之间的来源状态保持一致。

## 技术设计

- 统一 badge 和状态语义
- 在来源组件中展示当前资产阶段

## 影响范围

- `analysisProvenanceModel.ts`
- 多个页面来源面板

## 验证

- [ ] `pnpm --dir dts-copilot-webapp run typecheck`
  当前被工作区内未纳管的 `src/pages/screens/components/ComponentRenderer/EChartsSection.tsx` 既有 TS2698 错误阻塞。
- [x] `node --experimental-strip-types --test dts-copilot-webapp/tests/analysisProvenanceModel.test.ts dts-copilot-webapp/tests/copilotSessionFocus.test.ts`
- [x] `pnpm --dir dts-copilot-webapp run build`

## 完成标准

- [x] 用户能清楚知道当前资产处于哪一阶段
