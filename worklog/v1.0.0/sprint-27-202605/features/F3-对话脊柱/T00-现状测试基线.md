# T00: 现状测试基线

**优先级**: P0
**状态**: DONE
**依赖**: 无

## 目标

在删除旧 `/agent-bi` 页面、改造工作台壳、拆分 `CopilotChat` 之前，先把现有聊天主链路固化成可重复运行的基线测试，作为 F1/F3/F5 后续重构的回归闸门。

## 技术设计

本 Task 只补测试与最小纯函数抽取，不改变产品行为、不删除路由、不调整布局。

基线覆盖四条真实风险路径：

1. **发送守卫**：空输入、未启用 Copilot、发送中/流式进行中时不重复发起。
2. **SSE 事件归并**：`session` / `reasoning` / `token` / `tool` / `done` / `error` 对消息状态的影响可用纯 reducer 测试。
3. **停止与打断重发**：复用 `resolveCopilotSendAction`，锁住 `send` / `stop` / `interrupt-and-send` 三态。
4. **渲染契约**：消息区、输入区、`InlineSqlPreview`、`TracePanel`、`FeedbackButtons`、`VoiceInputButton` 的关键挂载点有结构断言，避免拆分时丢功能。

基线先基于当前 `src/components/copilot/CopilotChat.tsx` 建立；F3-T01/T02 拆分后再把断言目标迁移到 `ConversationThread` / `MessageList` / `Composer`，但行为断言保持同一套。

## 影响范围

- 新增或扩展：`src/components/copilot/CopilotChat.presentation.test.ts`。
- 新增：`src/components/copilot/copilotSendGuard.ts` / `copilotSendGuard.test.ts`。
- 新增：`src/components/copilot/copilotStreamReducer.ts` / `copilotStreamReducer.test.ts`。
- 复核：`copilotStreamControl.test.ts`、`copilotReasoningState.test.ts`、`copilotComposerState.test.ts`、`copilotInputBehavior.test.ts`、`copilotSessionBootstrap.test.ts`、`copilotSessionFocus.test.ts`。

## 验证

- [x] 补测试前记录一次当前 `pnpm test` 基线结果。
- [x] 新增测试覆盖发送守卫、SSE 归并、停止/打断重发、关键渲染挂载点。
- [x] 补测试后 `pnpm typecheck` 通过。
- [x] 补测试后 `pnpm test` 全量通过。
- [x] F1 删除旧路由前，本 Task 已完成并可作为回归闸门。

## 执行记录

- 2026-05-30: 新增 `copilotSendGuard` 与 `copilotStreamReducer` 纯逻辑护栏,并把 `CopilotChat` 的发送守卫、SSE 消息归并切到 helper。
- 2026-05-30: 扩展 `CopilotChat.presentation.test.ts`,锁住消息区、输入区、`InlineSqlPreview`、`TracePanel`、`FeedbackButtons`、`VoiceInputButton` 的拆分前挂载锚点。
- 2026-05-30: 新增 `copilotStreamControl.test.ts`,用 Vitest 覆盖 `send` / `stop` / `interrupt-and-send` 三态。
- 2026-05-30: `pnpm exec vitest run src/components/copilot/CopilotChat.presentation.test.ts src/components/copilot/copilotSendGuard.test.ts src/components/copilot/copilotStreamReducer.test.ts src/components/copilot/copilotStreamControl.test.ts src/components/copilot/WelcomeCard.test.tsx src/components/copilot/copilotGeneratedReportMessage.test.ts src/pages/screens/hooks/cardDataMapper.test.ts src/pages/screens/renderers/shared/actionUtils.test.ts src/pages/screens/renderers/shared/chartUtils.test.ts` 通过,9 个测试文件 / 37 个断言。
- 2026-05-30: `pnpm typecheck` 通过。
- 2026-05-30: 修复 React 19.2 + pnpm peer 目录下的 Vitest React 实例不一致问题:在 `vitest.config.ts` 中把测试期 `react` alias 到 `react-dom` 实际使用的 React,并把既有 React Testing Library 用例改为等待首屏提交/本地 Probe 组件。
- 2026-05-30: `pnpm test` 通过,27 个测试文件 / 119 个断言。

## 完成标准

- [x] 现状行为被测试固定，后续 F1/F3 改动必须先跑过这套基线。
- [x] `CopilotChat` 拆分前没有先删除旧入口或大范围搬代码。
- [x] 测试文件内标明拆分后的断言迁移目标，避免后续维护者误删护栏。
