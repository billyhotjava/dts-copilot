# T02: Sprint-31 IT 证据包

**优先级**: P2
**状态**: READY
**依赖**: F1-F4

## 目标

归档本 sprint 的真实、可重跑验证证据，使"语义已收口"可被复核而非口说。

## 技术设计

证据落 `it/`，至少包含：
- 三源差异矩阵（F1-T01 产出快照）
- 定源 ADR（F1-T04）链接
- sync 生成 diff（F2-T02：生成前后 pack guardrails 对比）
- 漂移检测触发样例 + 降级演练日志（F2-T03）
- 跨源一致性回归 + 铁律拦截回归运行日志（F4，绿）
- 草稿晋升闭环一例的全链路截图/日志（F3-T03）

每条证据带可重跑命令或脚本（沿用 sprint-30 `it/test_*.sh` 风格）。

## 影响范围

- 产出 `it/README.md` + `it/evidence/` + 可重跑脚本

## 验证

- [ ] 每条证据可被第三方按命令复现

## 完成标准

- [ ] `it/README.md` 证据齐全、可重跑、非占位
