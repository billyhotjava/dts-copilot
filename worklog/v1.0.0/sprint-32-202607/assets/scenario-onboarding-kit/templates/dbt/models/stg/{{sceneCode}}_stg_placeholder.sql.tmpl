-- {{domainName}} STG placeholder.
-- Replace this file with typed ODS transformations. STG must not contain business aggregation.

select
  cast(null as varchar) as source_id,
  cast(null as timestamp) as source_updated_at
where 1 = 0
