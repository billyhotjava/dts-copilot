# 库存 Trino catalog template
# Copy to dts-stack Trino catalog directory only after replacing secrets with env-driven values.
connector.name=mysql
connection-url=jdbc:mysql://${ENV:Inventory_MYSQL_HOST}:${ENV:Inventory_MYSQL_PORT}/rs_cloud_flower
connection-user=${ENV:Inventory_MYSQL_USER}
connection-password=${ENV:Inventory_MYSQL_PASSWORD}
case-insensitive-name-matching=true
