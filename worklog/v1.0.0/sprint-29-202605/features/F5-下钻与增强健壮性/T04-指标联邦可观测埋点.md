# T04: 指标联邦可观测埋点

**优先级**: P3
**状态**: DONE
**依赖**: F1、F3

## 落地

- `dts-copilot-ai` 新增 `IndicatorRoutingMetricsService`,记录 `copilot.indicator.routing.total{tier,has_candidates}`。
- `dts-copilot-analytics` 新增 `PlatformIndicatorMetricsService`,记录:
  - `platform.indicator.api.duration{operation,result}`
  - `platform.indicator.api.total{operation,result}`
  - `platform.indicator.cache.total{mode,result}`
- AI/analytics 均开放 `/actuator/metrics`。

## 验证

- `mvn test` PASS: `dts-copilot-ai` 191 tests, `dts-copilot-analytics` 106 tests。
