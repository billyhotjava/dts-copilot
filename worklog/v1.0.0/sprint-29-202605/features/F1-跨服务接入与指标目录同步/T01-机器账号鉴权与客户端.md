# T01: 机器账号鉴权 + PlatformIndicatorClient 骨架

**优先级**: P0
**状态**: READY
**依赖**: 无

## 目标

为 copilot-ai 建立连 dts-platform 的**单受限只读机器账号**鉴权通道与 HTTP 客户端骨架:

- 用 Keycloak **client-credentials**(或配置好的 service user)从 realm `S10` 换取 access token。
- 封装 `PlatformIndicatorClient`,以 `Authorization: Bearer <token>` 调 dts-platform `/api/governance/indicators*` 端点。
- 平台**零改码**:仅需运维为该 client/账号配置治理指标只读角色;copilot 单方面适配平台现有鉴权。
- 统一超时与重试策略,所有失败转为结构化结果而非裸异常(为 T04 降级铺路)。

本 Task 只交付**鉴权 + 客户端骨架 + `listPublishedIndicators` 一个目录方法**;取值方法在 T04,定时同步在 T02。

## 技术设计

### 配置(application.yml,新增 `copilot.platform.*` 命名空间)

对齐现有 `copilot.action.adminapi.*`(见 `HttpAdminApiActionClient`)与 `dts.copilot.llm.*` 的配置风格,新增:

```yaml
copilot:
  platform:
    indicator:
      base-url: ${PLATFORM_BASE_URL:http://dts-platform:8080}   # 指标端点根
      enabled: ${PLATFORM_INDICATOR_ENABLED:true}
      connect-timeout-seconds: ${PLATFORM_CONNECT_TIMEOUT:5}
      read-timeout-seconds: ${PLATFORM_READ_TIMEOUT:30}
      active-dept: ${PLATFORM_ACTIVE_DEPT:}                      # 可选,透传 X-Active-Dept
    auth:
      token-uri: ${PLATFORM_TOKEN_URI:http://dts-keycloak:8080/realms/S10/protocol/openid-connect/token}
      client-id: ${PLATFORM_CLIENT_ID:dts-copilot-indicator-reader}
      client-secret: ${PLATFORM_CLIENT_SECRET:}                 # 必填,环境变量注入,严禁硬编码
      grant-type: ${PLATFORM_GRANT_TYPE:client_credentials}
```

> realm `S10` 与 `issuer-uri` 已在 dts-platform `config/application.yml`(`spring.security.oauth2.resourceserver.jwt.issuer-uri: http://dts-keycloak:8080/realms/S10`)确认;token-uri 由同 realm 推导。

### 新增类

1. `service/platform/PlatformTokenProvider`(`@Component`)
   - 方法 `String currentBearerToken()`:POST `token-uri`,body `grant_type=client_credentials&client_id=...&client_secret=...`(`application/x-www-form-urlencoded`),解析 `access_token` + `expires_in`。
   - **token 缓存**:内存缓存 token,按 `expires_in` 提前 ~30s 过期重取(`volatile` 字段 + 同步刷新),避免每次调用都打 Keycloak。
   - 复用 `java.net.http.HttpClient`(同 `HttpAdminApiActionClient` 构造方式),`connectTimeout` 取配置值。
   - `client-secret` 缺失 / token 端点不可达 → 抛 `PlatformAuthException`(由 `PlatformIndicatorClient` 捕获转降级)。

2. `service/platform/PlatformIndicatorClient`(`@Component`)
   - 构造注入 `ObjectMapper` + `PlatformTokenProvider` + `@Value` 配置(对齐 `HttpAdminApiActionClient` 写法)。
   - 私有 `get(path, queryParams)`:构造 `HttpRequest`,加 `Authorization: Bearer`(来自 `PlatformTokenProvider`)、可选 `X-Active-Dept`、`timeout(read-timeout)`;**401/403 时刷新 token 重试一次**;`IOException`/超时返回结构化失败。
   - 本 Task 实现:`PlatformPage<IndicatorDto> listPublishedIndicators(int page, int size)` → `GET /api/governance/indicators?status=已发布&page=&size=`。
   - 解析 dts-platform 信封:列表端点 payload 形如 `{ data: { content:[...], total, page, size, totalPages } }`(平台 `ApiResponses.ok` 包一层 `data`),客户端剥壳取 `data.content` + 分页字段。

3. `service/platform/IndicatorDto`(copilot 侧轻量镜像 record)
   - 仅映射本期需要的字段:`id, code, name, category, definition, expressionSql, status, version, tags, aggregationType, measureField, dimensionFields, dateColumn, timeGrain, dataLevel, domain, owner`。
   - Jackson `@JsonIgnoreProperties(ignoreUnknown = true)`,平台 DTO 新增字段不影响 copilot。

### 重试与超时

- 连接超时 5s、读超时 30s(配置可调,默认对齐 `HttpAdminApiActionClient`)。
- 仅对 401/403 做一次 token 刷新重试;对 5xx/超时**不在本层重试**(交 T02 同步层做整体退避 + T04 降级),避免放大平台负载。

## 影响范围

- **新增**:`dts-copilot-ai/src/main/java/.../service/platform/PlatformTokenProvider.java`、`PlatformIndicatorClient.java`、`IndicatorDto.java`、`PlatformPage.java`(分页结果 record)、`PlatformAuthException.java`。
- **修改**:`src/main/resources/application.yml` 新增 `copilot.platform.*`;`pom.xml` 无需新增依赖(用 JDK `java.net.http` + 已有 Jackson)。
- **dts-platform**:零改码,仅运维侧在 Keycloak 建 client `dts-copilot-indicator-reader` 并授治理指标只读角色(开放问题之一,需与 dts-stack 运维确认)。
- 不触碰 `SecurityConfiguration` / `ApiKeyAuthFilter`(出站调用,非入站)。

## 验证

- **单元测试**(`PlatformTokenProviderTest`、`PlatformIndicatorClientTest`,JUnit5 + Mockito,可用 `WireMock`/`MockWebServer` 桩 token 端点与指标端点):
  - token 成功获取并缓存,`expires_in` 内复用、过期后重取。
  - `client-secret` 缺失抛 `PlatformAuthException`。
  - `listPublishedIndicators` 正确拼 `status=已发布` 与分页参数,正确剥 `data.content` 信封。
  - 401 触发一次刷新重试;二次仍 401 返回失败结构。
  - 超时/连接拒绝不抛裸异常,返回失败结构。
- **Live contract**(记入 `it/README.md`):在可达 dts-platform + Keycloak 的环境,实际换 token 并拉到一页 `已发布` 指标,贴 curl/响应片段为证。

## 完成标准

- [ ] `copilot.platform.*` 配置就位,`client-secret` 走环境变量,无任何硬编码密钥。
- [ ] `PlatformTokenProvider` 能用 client-credentials 从 realm `S10` 换 token 并缓存复用。
- [ ] `PlatformIndicatorClient.listPublishedIndicators` 能成功调 `/api/governance/indicators?status=已发布` 并返回 copilot 侧 `IndicatorDto` 分页结果。
- [ ] 401/403 自动刷新重试一次;超时/不可达返回结构化失败不抛裸异常。
- [ ] 单元测试覆盖鉴权、分页、信封剥壳、失败分支;live contract 证据入 `it/README.md`。
- [ ] dts-platform 无任何代码改动。
