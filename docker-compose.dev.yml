# Development mode: only infrastructure services
# Java apps run in IDE, webapp via pnpm dev
services:
  copilot-ai:
    profiles: ["full"]
  copilot-analytics:
    profiles: ["full"]
  copilot-webapp:
    profiles: ["full"]
  copilot-proxy:
    profiles: ["proxy"]
